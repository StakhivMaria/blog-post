# 🌸 POST BLOG MANAGEMENT APPLICATION

Вебзастосунок для керування контентом блогу, розроблений на Node.js з використанням архітектурного патерну MVC та MongoDB як сховища даних. Реалізація відповідає вимогам **Advanced Level** завдання.

## 💻 Технологічний стек

| Категорія | Технології | Призначення |
| :--- | :--- | :--- |
| **Сервер (Бекенд)** | Node.js, Express.js | Швидка та масштабована основа для REST API. |
| **База Даних** | MongoDB (через Mongoose) | Гнучке NoSQL сховище для постійного зберігання даних. |
| **Фронтенд** | HTML5, CSS3, Чистий JavaScript | Простий, не залежний від фреймворків інтерфейс. |
| **Конфігурація** | Dotenv | Безпечне управління змінними оточення (`MONGO_URI`). |

## 🏗️ Архітектура: MVC

Проєкт організовано за патерном Model-View-Controller для чіткого поділу відповідальності:

* **Model (`models/Post.js`):** Визначає структуру даних (схему Mongoose) та взаємодіє безпосередньо з MongoDB.
* **Controller (`controllers/postController.js`):** Містить логіку застосунку, обробляє запити HTTP (CRUD) та використовує Модель для роботи з даними.
* **Routes (`routes/postRoutes.js`):** Визначає URL-маршрути (`/api/posts`, `/api/posts/:id`) та скеровує запити до відповідних Контролерів.
* **View (`public/index.html`):** Клієнтський інтерфейс, який використовує JavaScript для взаємодії з API.

## 📝 Функціонал Застосунку

Застосунок надає повний функціонал управління постами:

1.  **Створення:** Створення нових постів через форму (POST /api/posts).
2.  **Перегляд:** Відображення списку всіх постів (GET /api/posts).
3.  **Редагування:** Отримання даних поста для форми (GET /api/posts/:id) та їх оновлення (PUT /api/posts/:id).
4.  **Видалення:** Видалення посту за його ID (DELETE /api/posts/:id).

---

## 🚀 Інструкція із запуску (Локально)

Виконайте ці кроки для запуску застосунку на вашому локальному комп'ютері.

### 1. Попередня підготовка

Клонуйте репозиторій та перейдіть у папку проєкту:

```bash
git clone [https://github.com/StakhivMaria/blog-post.git](https://github.com/StakhivMaria/blog-post.git)
cd blog-post
### 2. Встановлення залежностей
Bash

npm install
### 3. Налаштування MongoDB
Створіть файл .env у корені проєкту та вкажіть ваш рядок підключення до MongoDB Atlas:

# .env
MONGO_URI="mongodb+srv://<username>:<password>@<cluster-url>/<dbname>?retryWrites=true&w=majority" 
PORT=3000
Важливо: Не діліться цим файлом публічно.

### 4. Запуск сервера
Запустіть сервер у режимі розробки:

Bash

npm run dev
### 5. Доступ
Відкрийте у браузері: http://localhost:3000

☁️ Фіналізація та Деплой
Застосунок готовий до розгортання на хмарних платформах, таких як Render (рекомендовано), Cyclic або Netlify (для хостингу клієнтської частини, якщо API розміщено окремо).

Для деплою на Render, просто підключіть репозиторій та встановіть змінну оточення MONGO_URI.
